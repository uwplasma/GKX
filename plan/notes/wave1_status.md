# Wave 1 status

| Agent | Status | Verdict / key result |
|---|---|---|
| PR #44 tf32 audit | DONE | **Merge as-is.** All claims verified on CPU; mutation tests exact; baseline bit-identical; CI green; clean merge. Nits: PR-body wrong about `solve` (code safe via lu_solve internal pin); line-number allowlist brittle-but-loud. 9-item office-GPU checklist produced (incl. GEMV-tensor-core assumption check, re-run device-parity lanes post-merge). |
| PR #46 auto-fit-window | DONE | **Merge as-is.** Real defect (fit_signal="auto" silently ignored pinned [fit] tmin/tmax → e.g. γ=0.054 vs correct 0.095 on cyclone); fix verified bit-identical to explicit path; new tests fail on main with numeric mismatches (proper tests); CI green; 0 conflicts. Does NOT reduce auto-window horizon scatter (doesn't claim to): reproduced γ=0.1132 window [18.06,19.64] exactly. 0.3 REMAINDER now precisely scoped: (1) stationarity-tested windows (select_fit_window_loglinear unchanged; live scatter γ=0.062–0.113 across stride/horizon), (2) surface R²/uncertainty into RuntimeLinearResult + summary JSON (computed internally, never exposed), (3) γ·t_max<5 warning (nonexistent). Known debt: TEM lane needs window policy then tem_mismatch_table + audit + doc figures regenerate in one commit. |
| PR #47 residual gates | DONE | **Merge as-is, before krylov fix** (provably inert on the defective path — floor-neutralized run byte-identical). All 9 substitution sites verified; +121 lines; CI green; 0 conflicts. KEY: wrong-γ path = method="propagator" → dominant_eigenpair_propagator_cached which has NO residual gate at all (why nothing fail-closed). Caveats for maintainer: (1) runtime CLI state is UNCONDITIONALLY complex64 (initial_conditions.py:130,267,469) even under x64 → shift-invert outer gate now 119× looser (1.192e-4) on the path real users hit — deserves explicit ack; (2) at the floor, GMRES inner solve aims exactly at acceptance threshold (zero slack) — one-line fix: floor inner at 1e2*eps. Relayed both + drift data to krylov agent. |
| PR #45 gx-parity-matrix | running | |
| krylov fix (0.1) | DONE | **Root cause:** generic runtime default dispatched solver="krylov" to the UNGATED propagator branch (2 restarts × 24-dim Arnoldi on ONE imex2 step, dt=0.01 → growth separation e^0.001; returned Rayleigh quotient at relative residual 0.9993 = unconverged garbage; no certification anywhere on that branch). Dense ground truth (n=1024): ITG λ=+0.1027+0.1686j well isolated; certified adaptive path matches dense to 5 digits. **Fix (+165/−4, 4 files):** first-class method="adaptive" in dominant_eigenpair (residual-certified, RuntimeError on rejection), certifiable_residual_tolerance helper (PR #47-compatible semantics — one planned conflict block), generic contracts default to adaptive. After: physical ITG branch across scan, ky=0.3 γ=0.0889/ω=0.2802 (f32=x64), vs GX 0.1018/0.2868; ETG shift-invert path fails closed (pre-existing). ~2 min/ky. Regression test added (integration-marked, dense-anchored). patch: patches/krylov_fix.patch. **COLLATERAL DEFECT:** time-solver fit γ ~30% BELOW certified/dense eigenvalue at both resolutions even with 4–8× windows (0.070 vs 0.1027) — pre-existing time-path fit bias; GX agrees with the eigensolver. Relayed to fitrobust agent (compare vs certified ground truth, not just mutual consistency). |
| phase0 bundle (0.2/0.4/0.5/0.6) | DONE | **Implemented + verified.** patch: scratchpad/patches/phase0_bundle.patch (+161/−13, 12 files). 0.2: ensure_finite_linear_history (1e30 cap → FloatingPointError naming dt instability) on both fit paths + CFL startup warning via existing _linear_frequency_bound (repro: old confident γ=44.0 at |phi|=1.8e46 → now warning + exit 1; clean run silent; caveat: bound conservative, 1.7–2.4× exceedance warns on bounded runs). 0.4: --plot handles linear_scan via scan_comparison_figure (.scan.csv + summary.json both render). 0.5: [scan].ky added to QL example (verified live), 3 doc fixes. 0.6: jax>=0.10.1 + jaxlib>=0.10.1 + booz_xform_jax; core.py:337 comment corrected. Tests: 89 passed touched + 155 passed covering; ruff clean. |
| wout UX (1.1) | DONE | **Implemented + smoke-tested.** patch: scratchpad/patches/wout_ux.patch (+577/−8, 7 files). New src/gkx/workflows/runtime/wout.py (netCDF sniff on rmnc/zmns/xm/xn; default deck resolution; model="vmec" forcing; resolved-deck reproducer; output grouping ./<wout-stem>/gkx; --linear → 8-point ky scan). Extends direct_config_shorthand_args in place; --vmec/--vmex aliases. examples/common_input.toml (HSX/W7-X proven numerics, tprim=3 fprim=1, t_max=200 + run_to note) single-sourced via relative symlink into src/gkx/data/ + package-data glob (wheel build verified dereferences). 8 new CLI tests, 42/42 pass. REAL smoke: gkx wout_QHS.nc --steps 20 → exit 0 in 118 s, full chain (sniff → deck → eik gen → 20 adaptive steps → grouped outputs). Note: --linear path uses linear-default Nl/Nm (24/12). |
| stella study (2.3) | DONE | Already cloned at ~/local/stella (v0.5.1-240-g4cdc5fcd, 2023, ES-ONLY; master 2026 has EM + new namelists, 185 commits ahead — build master on office Linux for EM). REBUILT on macOS (old binary broken by MPICH upgrade) and validated bit-for-bit vs archived run: CBC linear ky=0.5 ω=0.18703+0.09703i. SAME normalization family as GX/GKX (a_ref=Aminor_p, B_ref=2psi/a², vth=√(2T/m)); set flux_norm=false for flux comparisons. Velocity mapping: nmu≥Nl exact; nvpa≈2–3×Nm. input.geometry overwrite hook can force IDENTICAL geometry into stella+GKX (decisive for parity). W7-X anchor: González-Jerez JPP 2022 + bundled wout_w7x_standardConfig.nc. Ladder: RH residual → CBC linear (adiab+kinetic e) → W7-X linear s=0.49 → nonlinear CBC flux. Full report: scratchpad/stella_study.md |
| office GX (2.1) | DONE | GX had been DELETED from office; re-cloned canonical bitbucket gyrokinetics/gx → /home/rjorge/GX @ 3865a537 (2026-08-11), 56 commits past reference bc2fe552 (ancestor confirmed). Built OK (new untracked Makefiles/Makefile.office, CUDA 11.5 + gcc-10.4, sm_86, NCCL 2.18.1). Physics-relevant upstream changes: bpar nonlinear-CFL fix, g0 gyroaverage fix, FLR/Bessel precision change, Wspectra input group REMOVED (−787 lines; old inputs inert), pyvmec sign fix, iota/pressure half/full-grid derivatives. Decision item: upstream Makefile.ubuntu now adds -prec-sqrt=true; office build kept old flags. AUTO-STOP: GX has NONE (while counter<nstep && t<t_max; only a .stop-file hook) → run_to="saturation" is a real GKX differentiator. MULTI-GPU: 1 rank=1 GPU, decomposes SPECIES then HERMITE only (spatial + Laguerre replicated), NCCL comms (global/s/m/m0), m±1,m±2 halo exchange for streaming (src/moments.cu ~350-440), field solve ncclAllReduce on m0 ranks (src/solver.cu:130). GPUs effectively free (~15.6 GiB/card). |
| jax venv (0.7) | running | ~/.venvs/gkx-jax-latest |

Worktrees: /Users/rogeriojorge/local/GKX-worktrees/{pr44,pr45,pr46,pr47,krylov,phase0,woutux}
Cleanup when done: `git worktree remove <path>` from /Users/rogeriojorge/local/GKX (keep changed ones until patches applied).

## Wave 2
| Agent | Status | Key result |
|---|---|---|
| 4.1 parallelization design | DONE | **Recommendation: GX-parity (species, hermite) 2-D shard_map mesh**, (ky,kx)+Laguerre+z replicated. Comms: masked psum for field moments (~25–42 MB/RHS), single width-2 ppermute for m±1/m±2 halo, collision psums, scalar diag psums in scan carry (118x lesson baked in). ky/kx rejected (FFT all-to-all ~8–10× shard volume; kx breaks twist-shift chains); z rejected honestly (4 z-nonlocal operators; |kz| has no finite halo). Laguerre-sharding dead (dense full-l transform in grid mode). Sanity on 2 logical CPUs: halo, masked psum, AND jax.grad through shard_map all bitwise-exact. Memory: production stellarator case 8.6–12.9 GB serial → 4.3–6.4 GB/GPU (A4000 fit). jax 0.10 note: pmap C++ infra removed → re-base species route on shard_map. Office 2×A4000 mesh is species-split, halo-free. Doc: scratchpad/parallelization_design.md |
| 2.2 finite-beta equilibria | running | |
| stella-vs-GKX CBC point | running | |

## Wave 2 queue (launch as capacity frees)
- 1.2 auto-stop run_to="saturation" (after #46 verdict; reuses Sokal IAT from tools/campaigns/heat_flux_autocorrelation.py)
- 1.3 auto-plot set + snapshots module promotion (after woutux patch lands to avoid cli.py conflicts)
- 2.2 finite-beta wout generation via vmex (pressure-only QA, pressure+current QA)
- 4.1 parallelization design study doc (needs office GX multi-GPU notes from 2.1 agent)
- 0.3 remainder (after #46 verdict says what's left)
