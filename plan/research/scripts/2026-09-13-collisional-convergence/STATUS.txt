Q8 collisional Laguerre convergence -- STATUS at second pause (2026-09-13 19:17 CDT)

State: PAUSED by the maintainer mid-campaign. Nothing pushed, no PR.
- Office run directory (kept): /home/rjorge/gkx-q8-collisional-convergence-20260913.H01e3Z
  (src_stage/ = git archive of 22d39e9 + the scripts in this directory,
  synced; results/, logs/, gx/ decks, validation/).
- Terminal state verified 2026-09-13T19:17:29-05:00: no gate, supervisor,
  parity-runner or GX process of this row; no compute process on either GPU.
  PIDs used and verified gone: supervisor 260994 (exit 4 at 19:05:07, busy
  GPU guard); runner timeout/python 261215/261218 (GPU preflight),
  261922/261925, 272886/272889, 289413/289416; gate 310799 (killed in its
  sleep, nothing launched). CPU preflights ran in the foreground (terminal).
- Source: origin/main 578b97074 (physics diff vs Q3's 06606e404 empty, so
  Q3's nu1e-3/nu1e-2 x Nl24/32 runs and collisionless Nl24/32/48 are reused;
  see q3_reused.csv).

Completed (GKX, office GPU1, f64, T=150, fit [105,150]; runner settled flag)
  preflight-nu3e-3-nl24 (500 steps, GPU)  exit 0, 13 s
  nu3e-3-nl24   gamma .0234140  omega .496538  half shift +7.4e-4  settled  5:41.7
  nu3e-3-nl32   gamma .0174873  omega .495787  half shift -2.1e-4  settled
  nu1e-3-nl48   gamma .0200511  omega .494403  half shift -9.7e-3  settled  12:04.0
  (reused Q3: nu1e-3 Nl24 .0291274, Nl32 .0174118; nu1e-2 Nl24 .0174378,
   Nl32 .0171657; collisionless Nl24 .0328280, Nl32 .0249958*, Nl48 .0197718*,
   * = not settled)
CPU preflights: built-in collision term resolves for nu 0/1e-3/3e-3/1e-2,
RHS change linear in nu (ratios 3.000000, 10.000000) -> PASS.
P3: collision_operator="sugama" is REJECTED at Nl32 x Nm96 by
src/gkx/solvers_time_runners.py:85-115 (8-moment matrix vs 3072 moments),
both in preflight_collisions.py and through the parity runner (exit 1, 4.2 s);
it resolves only on its 2 x 4 basis. P3 is not runnable on main -> report as
not run with that reason.
GX fit script validated on the existing collisionless GX Nl24 T=300 output:
gamma(Phi2, [210,300]) .0330094, omega .500247 (matches the recorded .033009).

Already observable (not the final verdict): nu=1e-3 moves +15.2% from Nl32
(.01741) to Nl48 (.02005), so P1's |g48-g32|/g < 2% already fails for nu=1e-3.

Remaining (not started)
  GKX: nu3e-3-nl48, nu1e-2-nl48, nu1e-3-nl16, nu3e-3-nl16, nu1e-2-nl16,
       nu0-nl64; then the -t300 variant of any of these (or of the
       completed Q8 keys) whose settled flag is false.
  GX (only after GKX): gx-nu1e-2-nl24, gx-nu1e-2-nl32, gx-nu1e-2-nl32-nohyper
       (decks in gx/, diffs in gx/deck_diffs.txt: only t_max 150,
       save_for_restart/fields/moments false, vnewk 1e-2, nlaguerre, and
       hypercollisions false for -nohyper).
  Wait budget used: ~6 min (18:32:45-18:38:51) + ~12 min (19:05:07-19:17:17);
  remaining of the 3 h cap ~2 h 42 min -> WAIT_CAP 9600 s.
  Expected GPU time: GKX ~75 min (+ any T=300 reruns), GX ~50 min.

GPU occupancy history: GPU0 was repeatedly used by other sessions (lmx-gpu
2234 MiB at 18:36; vmex-gpu 5374 MiB at 19:03-19:07); GPU1 was taken by
another session's lmx-gpu pytest (PID 303210) exactly as nu1e-3-nl48
finished, which the per-key guard in run_convergence.sh caught (never shared).

Resume steps
1. git -C /Users/rogeriojorge/local/GKX-worktrees/perf-review fetch origin &&
   git -C /Users/rogeriojorge/local/GKX-worktrees/perf-review diff --stat \
     578b97074 origin/main -- src tools tests pyproject.toml
   Empty -> keep the staging; otherwise rerun the reused cases and say so.
2. Confirm the staged scripts equal the committed ones:
   ssh office 'cd /home/rjorge/gkx-q8-collisional-convergence-20260913.H01e3Z/src_stage && sha256sum plan/research/scripts/2026-09-13-collisional-convergence/*.{py,sh,toml}'
   against `shasum -a 256` of this directory. The staged manifest.toml,
   gate.sh and summarize.py are OLDER than the committed ones (key
   collisionless-nl64 renamed nu0-nl64 after the pause; committed manifest
   4822173c..., gate.sh ce5c51b5...): scp all *.py *.sh *.toml of
   this directory into src_stage before launching.
3. Launch the gate (it recomputes remaining keys from results/ and gx/, polls
   both GPUs every 300 s, needs two consecutive idle polls, runs GKX base keys,
   then unsettled -t300 reruns, then GX; aborts on any non-busy failure):
   ssh office 'R=/home/rjorge/gkx-q8-collisional-convergence-20260913.H01e3Z; D=plan/research/scripts/2026-09-13-collisional-convergence; (setsid nohup bash $R/src_stage/$D/gate.sh $R 9600 > $R/logs/gate.nohup.txt 2>&1 < /dev/null &); pgrep -af "[g]ate.sh"'
   Record the gate PID. Poll logs/gate.txt, logs/supervisor.txt and
   logs/gx_supervisor.txt yourself about every 5 min.
4. To pause again: kill -TERM -- -<gate pid>; then kill -KILL -- -<gate pid>
   after 10 s; a GX run is its own session (setsid), so also
   pkill -TERM -f '[v]vmgDD/gx gx-nu' and KILL after 10 s; verify with
   nvidia-smi --query-compute-apps and ps.
5. When the gate logs ALL DONE: pull results/*.json|csv, logs/*.txt (strip
   cpu_aot_loader lines), gx/*.json, gx/deck_diffs.txt, gx/*.in; trim each
   logs/gx-*.run.log to head/tail as *.txt; run
   python summarize.py RUN_DIR summary.csv > summary.txt locally on the pulled
   copy; delete gx/*.out.nc, *.big.nc, *.restart.nc and src.tgz on office;
   rm /private/tmp/gkx-q8-src.tgz locally.
6. Record: move log_entry_draft.txt (setup section, complete) plus tables,
   verdicts P1-P4, limitations and terminal job state into ONE entry at the
   END of plan/log.md, then delete the draft and this STATUS file; add one or
   two sentences at the end of plan.md 0.5's 2026-09-12 paragraph block;
   SHA256SUMS.txt; total < 300 KB. Checks: ruff 0.16.4 check + format --check,
   tools/release/check_repository_size_manifest.py, release gates + evidence
   ledger tests, gitleaks 8.30.x on the changed files (rename flagged
   identifiers, never suppress). Commit as Rogerio Jorge, no trailer; push;
   PR "evidence: collisional Laguerre convergence of the Cyclone ky=0.55
   growth rate (Q8)" with the plan 14 template; do not merge.

Gitleaks renames after the pause (no suppression): manifest key
collisionless-nl64(-t300) -> nu0-nl64(-t300) (never run); the validation
label validation-gx-nl24-collisionless-t300 -> validation in the committed
logs/validation_gx_fit.txt and validation/*.json (office copies keep the
executed label; no numeric change).
